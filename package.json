{
  "name": "direct-install",
  "devDependencies": {
    "tsup": "^6.6.3",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "build": "tsup index.ts --dts",
    "prepare": "npm run build"
  },
  "files": [
    "dist"
  ]
}
